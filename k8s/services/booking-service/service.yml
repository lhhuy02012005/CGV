apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: cgv-prod
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
