apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: cgv-prod
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
    - name: grpc
      port: 9090
      targetPort: 9090