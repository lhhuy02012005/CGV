apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: cgv-prod
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
