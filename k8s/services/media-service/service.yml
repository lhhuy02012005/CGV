apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: cgv-prod
spec:
  type: ClusterIP
  selector:
    app: media-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
