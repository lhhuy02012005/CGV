apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: cgv-prod
spec:
  type: ClusterIP
  selector:
    app: identity-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
