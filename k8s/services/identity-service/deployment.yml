apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: cgv-prod
  labels:
    app: identity-service
spec:
  replicas: 1 # Chạy 1 Pod (Xác thực JWT nhẹ, ít tải DB)
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: cgv/identity-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8081
          envFrom:
            - configMapRef:
                name: cgv-global-config
            - secretRef:
                name: cgv-global-secrets
          resources:
            requests:
              cpu: "80m"
              memory: "150Mi"
            limits:
              cpu: "400m"
              memory: "300Mi"
          readinessProbe:
            httpGet:
              path: /api/v1/actuator/health/readiness
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/actuator/health/liveness
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 15
